/// Task 1.1:
/// This function should return the sum of the two arguments
fn add(first: i32, second: i32) -> i32 {
    todo!()
}

/// Task 2.2:
/// This function should work like `value++`.
/// It should increment the original variable, then return the value as it was when the function was called.
///
/// Note: You might need to change the function signature.
fn plusplus(value: i32) -> i32 {
    todo!()
}

#[cfg(test)]
mod tests {
    use crate::task1_basics::*;

    #[test]
    fn test_task1_1_add() {
        //assert_eq!(add(4, 3), 7);
    }

    #[test]
    fn test_task1_2plusplus() {
        //let mut x = 5;
        //let result: i32 = plusplus(&mut x);

        //assert_eq!(x, 6);
        //assert_eq!(result, 5);
    }
}
